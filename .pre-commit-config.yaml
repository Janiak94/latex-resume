repos:
  - repo: local
    hooks:
      - id: add-build-file
        name: Add build file
        entry: bash -c 'cp build/main.pdf resume.pdf && git add resume.pdf'
        language: system
        stages: [pre-commit]
